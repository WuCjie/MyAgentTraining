## 第二天

## 目标
完成LLM API的面向对象封装，实现基础对话和多轮对话功能。

## 核心知识点

### 1. API封装设计
- 使用类封装LLM调用逻辑
- 支持从.env文件或传参初始化
- 直接传参给`ChatOpenAI`，不污染全局环境变量

### 2. 初始化参数优先级
传入参数 > 环境变量 > .env文件


### 3. 多轮对话原理
- 维护`history`列表存储对话历史
- 每次调用将完整历史传给LLM
- 历史格式：`[{"role": "user"/"assistant", "content": "..."}]`